# Core Dependencies
anthropic>=0.40.0
claude-agent-sdk>=0.1.9
requests>=2.31.0
python-dotenv>=1.0.0

# For testing and comparison
colorama>=0.4.6
tabulate>=0.9.0

# Observability (recommended - see what really happens under the hood)
# Laminar uses a Rust proxy to capture ALL SDK communications
lmnr[claude-agent-sdk]>=0.7.24
